import { css } from '@emotion/css'

export const wrapper = css`
  max-width: 600px;
  margin: auto;
`
export const pagination = css`
  width: fit-content;
  margin: auto;
  & > * {
    margin: 15px;
  }
`